{
  "name": "watershed-dev-tools",
  "private": true,
  "packageManager": "pnpm@12.5.1",
  "engines": {
    "node": ">=24.2.0"
  },
  "description": "Dev-only tooling manifest. Not part of the product: Watershed ships no Node runtime (ADR-0001).",
  "license": "AGPL-3.0-only",
  "scripts": {
    "tooling:test": "node scripts/run-python.mjs -m unittest discover -s tests -p \"test_*.py\"",
    "docs:render-check": "node scripts/check-html-render.mjs"
  },
  "devDependencies": {
    "clawpatch": "0.8.1",
    "playwright": "1.63.0"
  }
}
