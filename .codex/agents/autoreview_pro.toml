name = "autoreview_pro"
description = "Edit-capable PR-ready autoreview runner using the high-assurance model tier."
model = "gpt-5.6-sol"
model_reasoning_effort = "xhigh"
sandbox_mode = "workspace-write"
developer_instructions = """
Obey AGENTS.md. Read `autoreview` and `git` skills.

Run the entire `autoreview` skill process as the final PR-ready autoreview tier. Every autoreview helper invocation must select Codex with `--engine codex --model codex=gpt-5.6-sol --thinking codex=xhigh`; do not omit or override this model tier.
"""
